import { SpacefromtopDirective } from './spacefromtop.directive';

describe('SpacefromtopDirective', () => {
  it('should create an instance', () => {
    const directive = new SpacefromtopDirective();
    expect(directive).toBeTruthy();
  });
});
